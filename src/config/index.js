export const attributeName = 'data-rules';

export const formEvents = ['change', 'blur', 'focus', 'keydown', 'keypress', 'keyup'];

export const onShow = (root) => root.style.display = 'block'; // eslint-disable-line

export const onHide = (root) => root.style.display = 'none'; // eslint-disable-line
